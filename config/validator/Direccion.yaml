# ojo, no se ponen las variables que hacen referencia a relaciones
# Y no es obligatorio poner todas ls variables

App\Entity\Direccion:
  properties:
    calle:
      - NotBlank: ~
    cod_postal:
      - NotBlank: ~
    numero:
      - NotBlank: ~

